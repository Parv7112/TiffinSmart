import React from 'react';

const Spinner = () => {
  return (
    <div >
      <img src="assets/images/loading.gif"  alt="loading data from the server" />
    </div>
  );
};

export default Spinner;
